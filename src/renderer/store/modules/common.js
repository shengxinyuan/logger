const state = {
  userInfo: {}
}

const getters = {
  
}

const mutations = {
  common_setUserInfo(state, res) {
    state.userInfo = res
  }
}

const actions = {
  
}

export default {
  state,
  getters,
  actions,
  mutations
}
