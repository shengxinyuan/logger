const state = {
  loggerList: {}
}

const getters = {
  
}

const mutations = {
  logger_setLoggerList(state, res) {
    state.loggerList = res
  }
}

const actions = {
  
}

export default {
  state,
  getters,
  actions,
  mutations
}
